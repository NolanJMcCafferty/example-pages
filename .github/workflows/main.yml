name: proselint
on: [push]

jobs: 

  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
    
      - name: try
        working-directory: docs
        run: |
          ls -la
          echo help
        
  
  
